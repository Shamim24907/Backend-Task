<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Register — Dreamy Website</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #ffe4f0, #fff1e6);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
      position: relative;
      overflow: hidden;
    }

    /* Floating petals */
    .petal {
      position: absolute;
      width: 20px;
      height: 20px;
      background: rgba(255,182,193,0.4);
      border-radius: 50% 50% 50% 50%;
      animation: drift 15s infinite linear;
      pointer-events: none;
    }
    .petal:nth-child(1){top:5%; left:10%; animation-delay:0s;}
    .petal:nth-child(2){top:20%; right:15%; animation-delay:4s;}
    .petal:nth-child(3){top:50%; left:60%; animation-delay:8s;}
    .petal:nth-child(4){bottom:10%; left:30%; animation-delay:12s;}
    @keyframes drift {
      0%{transform: translateY(0) rotate(0deg);}
      50%{transform: translateY(30px) rotate(45deg);}
      100%{transform: translateY(0) rotate(360deg);}
    }

    .register-card {
      background: rgba(255,255,255,0.75);
      backdrop-filter: blur(15px);
      border-radius: 2rem;
      padding: 2rem;
      width: 100%;
      max-width: 400px;
      box-shadow: 0 15px 40px rgba(255,182,193,0.3);
      position: relative;
      z-index: 10;
      transition: transform 0.3s ease;
    }
    .register-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 25px 60px rgba(255,182,193,0.4);
    }

    h1 {
      font-family: 'Great Vibes', cursive;
      font-size: 3rem;
      color: #ff85a2;
      text-align: center;
      margin-bottom: 0.5rem;
    }
    p.subtitle {
      text-align: center;
      color: #ffb6b9;
      margin-bottom: 1.5rem;
      font-weight: 500;
    }

    .input-field {
      width: 100%;
      padding: 0.75rem 1rem;
      border-radius: 1rem;
      border: 2px solid rgba(255,182,193,0.3);
      margin-bottom: 1rem;
      transition: all 0.3s ease;
      background: rgba(255,255,255,0.6);
    }
    .input-field:focus {
      outline: none;
      border-color: #ff85a2;
      background: rgba(255,255,255,0.8);
    }

    .register-btn {
      width: 100%;
      padding: 0.75rem;
      border-radius: 9999px;
      font-weight: 600;
      background: linear-gradient(135deg, #ff85a2, #ffb6b9);
      color: white;
      transition: all 0.3s ease;
      font-size: 1.1rem;
      cursor: pointer;
    }
    .register-btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 30px rgba(255,133,162,0.4);
    }

    .terms { font-size: 0.85rem; color: #ff7ea0; margin-bottom:1rem; }
    .terms a { color: #ff85a2; text-decoration: underline; }

    .login-link { text-align: center; margin-top:1rem; color:#ff85a2; }
    .login-link a { text-decoration: underline; font-weight:500; }
  </style>
</head>
<body>

  <!-- Floating petals -->
  <div class="petal"></div>
  <div class="petal"></div>
  <div class="petal"></div>
  <div class="petal"></div>

  <div class="register-card">
    <h1>Register Now</h1>
    <p class="subtitle">Sign up and join our soft, dreamy community</p>

    <form method="POST" action="/register">
      <input type="text" name="name" placeholder="Full Name" required class="input-field">
      <input type="email" name="email" placeholder="Email Address" required class="input-field">
      <input type="password" name="password" placeholder="Password" required class="input-field">

      <div class="terms">
        <input type="checkbox" required /> I agree to the <a href="/terms">Terms</a> and <a href="/privacy">Privacy Policy</a>
      </div>

      <button type="submit" class="register-btn">Create Account</button>
    </form>

    <p class="login-link">Already have an account? <a href="/login">Login</a></p>
  </div>

</body>
</html>
