<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Register - Shamim</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #fdf6fb, #e0f7fa);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
      position: relative;
      overflow: hidden;
    }

    /* Floating shapes */
    .shape {
      position: absolute;
      border-radius: 50%;
      opacity: 0.12;
      animation: float 25s infinite alternate;
      pointer-events: none;
    }
    .shape-1 { width: 160px; height: 160px; top: 10%; left: 5%; background: #ffd6e0; animation-delay: 0s;}
    .shape-2 { width: 120px; height: 120px; bottom: 15%; right: 10%; background: #a0e9ff; animation-delay: 5s;}
    .shape-3 { width: 200px; height: 200px; top: 50%; left: 50%; background: #d6f6d6; animation-delay: 10s;}
    @keyframes float {
      0% { transform: translateY(0) translateX(0); }
      50% { transform: translateY(-25px) translateX(15px); }
      100% { transform: translateY(0) translateX(0); }
    }

    /* Glass Card */
    .register-card {
      backdrop-filter: blur(20px);
      background: rgba(255,255,255,0.35);
      border-radius: 2rem;
      border: 1px solid rgba(255,255,255,0.25);
      box-shadow: 0 12px 35px rgba(0,0,0,0.08);
      padding: 2.5rem;
      width: 100%;
      max-width: 420px;
      transition: all 0.3s ease;
      position: relative;
      z-index: 10;
    }
    .register-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 15px 50px rgba(0,0,0,0.12);
    }

    /* Logo */
    .logo {
      width: 48px;
      height: 48px;
      background: linear-gradient(135deg, #fd79a8 0%, #fab1a0 100%);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 24px;
      font-weight: bold;
      color: white;
      margin: 0 auto 1rem;
    }

    /* Inputs */
    .input-field {
      background: rgba(255,255,255,0.6);
      border: none;
      border-radius: 0.75rem;
      padding: 0.875rem 1rem;
      transition: all 0.3s ease;
      color: #333;
      font-size: 0.95rem;
      width: 100%;
      margin-bottom: 1rem;
    }
    .input-field:focus {
      background: rgba(255,255,255,0.8);
      outline: none;
      border: 1px solid #ff85a2;
    }
    .input-field::placeholder {
      color: #999;
    }

    /* Buttons */
    .register-btn {
      background: linear-gradient(135deg, #fd79a8 0%, #fab1a0 100%);
      color: #fff;
      font-weight: 600;
      border-radius: 0.75rem;
      width: 100%;
      padding: 0.875rem;
      transition: all 0.3s ease;
      border: none;
      cursor: pointer;
      font-size: 1rem;
    }
    .register-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 30px rgba(253,121,152,0.35);
    }

    /* Terms */
    .terms {
      font-size: 0.875rem;
      color: #666;
      margin-bottom: 1rem;
      display: flex;
      align-items: start;
      gap: 0.5rem;
    }
    .terms input[type="checkbox"] {
      margin-top: 0.25rem;
      cursor: pointer;
      width: 16px;
      height: 16px;
      flex-shrink: 0;
    }
    .terms a {
      color: #ff85a2;
      font-weight: 500;
      transition: all 0.2s ease;
    }
    .terms a:hover {
      color: #fd79a8;
      text-decoration: underline;
    }

    /* Login link */
    .login-link {
      text-align: center;
      margin-top: 1rem;
      color: #666;
      font-size: 0.875rem;
    }
    .login-link a {
      color: #ff85a2;
      font-weight: 500;
      transition: all 0.2s ease;
    }
    .login-link a:hover {
      color: #fd79a8;
      text-decoration: underline;
    }

    label {
      font-weight: 500;
    }
  </style>
</head>
<body>

  <!-- Floating shapes -->
  <div class="shape shape-1"></div>
  <div class="shape shape-2"></div>
  <div class="shape shape-3"></div>

  <div class="register-card">
    <!-- Logo -->
    <div class="logo">S</div>
    
    <h1 class="text-3xl font-bold text-gray-800 text-center mb-2">Create Account</h1>
    <p class="text-center text-gray-600 text-sm mb-6">Join our community today</p>

    <form method="POST" action="/register">
      <div>
        <label class="block text-gray-700 text-sm mb-2">Full Name</label>
        <input type="text" name="name" placeholder="Enter your full name" required class="input-field">
      </div>

      <div>
        <label class="block text-gray-700 text-sm mb-2">Email Address</label>
        <input type="email" name="email" placeholder="you@example.com" required class="input-field">
      </div>

      <div>
        <label class="block text-gray-700 text-sm mb-2">Password</label>
        <input type="password" name="password" placeholder="Create a strong password" required class="input-field">
      </div>

      <div class="terms">
        <input type="checkbox" required id="terms" />
        <label for="terms" class="cursor-pointer">
          I agree to the <a href="/terms">Terms of Service</a> and <a href="/privacy">Privacy Policy</a>
        </label>
      </div>

      <button type="submit" class="register-btn">Create Account</button>
    </form>

    <p class="login-link">Already have an account? <a href="/login">Sign In</a></p>
  </div>

</body>
</html>