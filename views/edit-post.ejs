<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Post - Modern UI</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(120deg, #f7f7f7, #e0f7fa);
      min-height: 100vh;
      padding: 2rem;
      position: relative;
    }

    /* Decorative floating shapes */
    .shape {
      position: absolute;
      border-radius: 50%;
      opacity: 0.1;
      pointer-events: none;
      animation: float 25s infinite alternate;
    }
    .shape-1 { width: 200px; height: 200px; top: 5%; left: 10%; background: #ffb6b9; animation-delay: 0s;}
    .shape-2 { width: 150px; height: 150px; bottom: 10%; right: 5%; background: #81ecec; animation-delay: 8s;}
    .shape-3 { width: 220px; height: 220px; top: 40%; left: 50%; background: #ffeaa7; animation-delay: 15s;}
    @keyframes float {
      0% { transform: translateY(0) translateX(0); }
      50% { transform: translateY(-25px) translateX(15px); }
      100% { transform: translateY(0) translateX(0); }
    }

    /* Glass card */
    .glass-card {
      backdrop-filter: blur(20px);
      background: rgba(255, 255, 255, 0.55);
      border-radius: 1.5rem;
      border: 1px solid rgba(255,255,255,0.25);
      box-shadow: 0 12px 35px rgba(0,0,0,0.08);
      transition: all 0.3s ease;
    }
    .glass-card:hover {
      box-shadow: 0 15px 40px rgba(0,0,0,0.12);
    }

    .textarea-field {
      background: rgba(255,255,255,0.75);
      border: 1px solid rgba(0,0,0,0.12);
      border-radius: 1rem;
      min-height: 160px;
      padding: 1.2rem;
      resize: vertical;
      transition: all 0.3s ease;
    }
    .textarea-field:focus {
      outline: none;
      border-color: #fd79a8;
      background: rgba(255,255,255,0.9);
      box-shadow: 0 0 10px rgba(253,121,152,0.3);
    }

    .btn-primary {
      background: linear-gradient(135deg, #fd79a8, #fab1a0);
      color: #fff;
      font-weight: 600;
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px rgba(253,121,152,0.35);
    }

    .btn-secondary {
      background: rgba(255,255,255,0.7);
      border: 1px solid rgba(0,0,0,0.15);
      color: #333;
      transition: all 0.3s ease;
    }
    .btn-secondary:hover {
      background: rgba(255,255,255,0.85);
      transform: translateY(-2px);
    }

    .file-input-wrapper {
      border: 2px dashed rgba(0,0,0,0.15);
      background: rgba(255,255,255,0.4);
      padding: 1.2rem;
      border-radius: 1rem;
      text-align: center;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    .file-input-wrapper:hover {
      background: rgba(255,255,255,0.6);
      border-color: #fd79a8;
    }
    .file-input {
      opacity: 0;
      position: absolute;
      width: 100%;
      height: 100%;
      cursor: pointer;
    }
  </style>
</head>
<body class="relative">

  <!-- Decorative Shapes -->
  <div class="shape shape-1"></div>
  <div class="shape shape-2"></div>
  <div class="shape shape-3"></div>

  <div class="max-w-3xl mx-auto mt-10 relative z-10 space-y-8">

    <!-- Edit Post Card -->
    <div class="glass-card p-10 space-y-6">
      <div class="flex items-center justify-between mb-4">
        <h1 class="text-3xl font-bold text-gray-800">Edit Post</h1>
        <a href="/profile" class="btn-secondary px-5 py-2 rounded-full text-sm">← Back</a>
      </div>

      <form method="POST" action="/posts/edit/<%= post.id %>" enctype="multipart/form-data" class="space-y-6">
        <div>
          <label class="block text-gray-700 font-medium mb-2">Content</label>
          <textarea name="content" maxlength="2000" required class="textarea-field w-full text-gray-800 placeholder-gray-500" placeholder="Update your post..."><%= post.content %></textarea>
          <p class="text-gray-500 text-sm mt-1 flex justify-between">
            <span>Characters: <%= post.content.length %></span>
            <span>Max: 2000</span>
          </p>
        </div>

        <div class="space-y-4">
          <% if(post.imagePath) { %>
          <div class="border rounded-lg p-4 bg-white/60">
            <h4 class="text-gray-700 font-medium mb-2">Current Image</h4>
            <img src="<%= post.imagePath %>" alt="Post Image" class="object-contain max-h-56 w-full rounded-md mb-2">
            <label class="flex items-center gap-2 text-red-500 cursor-pointer text-sm">
              <input type="checkbox" name="remove_image" value="true" class="h-4 w-4 rounded border-gray-300">
              Remove current image
            </label>
          </div>
          <% } %>

          <div class="file-input-wrapper relative flex flex-col items-center justify-center cursor-pointer">
            <svg class="w-7 h-7 text-gray-700 mb-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
            </svg>
            <span class="text-gray-700 text-sm font-medium">Click to upload/replace image</span>
            <input type="file" name="image" class="file-input" accept="image/*">
          </div>
        </div>

        <div class="flex gap-4 pt-4">
          <button type="submit" class="btn-primary flex-1 py-3 rounded-full text-lg font-semibold">Save Changes</button>
          <a href="/profile" class="btn-secondary flex-shrink-0 px-6 py-3 rounded-full text-sm">Cancel</a>
        </div>
      </form>
    </div>

    <!-- Tips Card -->
    <div class="glass-card p-6">
      <h3 class="text-gray-800 font-semibold mb-3">Quick Tips</h3>
      <ul class="text-gray-600 text-sm list-disc list-inside space-y-1">
        <li>Keep your post concise and engaging.</li>
        <li>Use spacing for readability.</li>
        <li>You can replace or remove your image as needed.</li>
      </ul>
    </div>

  </div>

</body>
</html>
